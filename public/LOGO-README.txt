Site logo for Navbar / Footer / heroes:

  logo.jpg  — use this in code (Next.js Image + Vercel)

  logoelec.jfif — legacy file; .jfif is NOT supported by Next.js
  Image optimization on Vercel (broken logo in production).

To update the logo: replace logo.jpg (JPEG or PNG renamed to .jpg).

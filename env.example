# Copy to `.env.local` and set your API base URL (no trailing slash).
# Postman: {{base_url}}/auth/login → e.g. https://api.example.com/api
NEXT_PUBLIC_API_BASE_URL=https://www.urgentelectrical.services/api
